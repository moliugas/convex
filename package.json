{
  "name": "my-convex-app",
  "version": "1.0.0",
  "description": "Convex backend project",
  "scripts": {
    "dev": "npm install convex@latest",
    "deploy": "convex deploy"
  },
  "dependencies": {
    "convex": "^1.16.0"
  },
  "devDependencies": {
    "convex-dev": "^1.16.0"
  }
}